{
  "name": "hawknet-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter app tauri dev",
    "build": "pnpm --filter app tauri build",
    "preview": "pnpm --filter app preview",
    "tauri": "pnpm --filter app tauri"
  }
}